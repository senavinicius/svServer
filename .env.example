PORT=3100
NODE_ENV=development

# ===== CLIENTE (Frontend Vite) =====
# URL da API backend quando roda separado do frontend
# - OBRIGATÓRIO em dev mode
# - Em produção, o domínio é detectado automaticamente (window.location.origin)
VITE_API_URL=http://localhost:3100

# ===== AUTENTICAÇÃO (Google OAuth) =====
# Todas as variáveis abaixo são OBRIGATÓRIAS
# Para obter credenciais: https://console.cloud.google.com

# ID do cliente OAuth (termina com .apps.googleusercontent.com)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Secret do cliente OAuth (começa com GOCSPX-)
GOOGLE_CLIENT_SECRET=GOCSPX-your-secret

# String aleatória para assinar tokens JWT (MÍNIMO 32 caracteres)
# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH_SECRET=your-random-secret-string-minimum-32-chars

# Caminho (PATH) onde as rotas OAuth vão responder
# ❌ ERRADO: AUTH_GOOGLE_CALLBACK_PATH=https://seudominio.com/auth
# ✅ CORRETO: AUTH_GOOGLE_CALLBACK_PATH=/auth
#
# Este é APENAS o path (começa com /), NÃO a URL completa!
# O domínio é detectado automaticamente da requisição.
#
# IMPORTANTE: O cliente usa /auth/signin no botão de login.
# Se você mudar este valor, precisa mudar no cliente também (src/client/main.ts:108)
#
# Este path cria as rotas:
# - /auth/signin (iniciar login - USADO PELO CLIENTE)
# - /auth/callback/google (callback do Google - configure no Google Console)
# - /auth/signout (logout)
#
# Configure no Google Console a URI: https://seudominio.com/auth/callback/google
AUTH_GOOGLE_CALLBACK_PATH=/auth
