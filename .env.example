PORT=3100
NODE_ENV=development

# ===== CLIENTE (Frontend Vite) =====
# URL da API backend quando roda separado do frontend
# - Opcional: se não definir, usa window.location.origin
# - Use apenas em dev mode quando backend roda em porta diferente
VITE_API_URL=http://localhost:3100

# Base path para rotas de autenticação (DEVE SER IGUAL AO DO SERVIDOR)
# Se você configurou AUTH_GOOGLE_CALLBACK_PATH=/googleLogin no servidor,
# configure aqui também: VITE_AUTH_CALLBACK_PATH=/googleLogin
# Default: /auth
VITE_AUTH_CALLBACK_PATH=/googleLogin

# ===== AUTENTICAÇÃO (Google OAuth) =====
# Todas as variáveis abaixo são OBRIGATÓRIAS
# Para obter credenciais: https://console.cloud.google.com

# ID do cliente OAuth (termina com .apps.googleusercontent.com)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Secret do cliente OAuth (começa com GOCSPX-)
GOOGLE_CLIENT_SECRET=GOCSPX-your-secret

# String aleatória para assinar tokens JWT (MÍNIMO 32 caracteres)
# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH_SECRET=your-random-secret-string-minimum-32-chars

# BASE PATH para rotas OAuth (DEVE SER IGUAL AO DO CLIENTE)
# ❌ ERRADO: AUTH_GOOGLE_CALLBACK_PATH=https://seudominio.com/googleLogin
# ✅ CORRETO: AUTH_GOOGLE_CALLBACK_PATH=/googleLogin
#
# Este é APENAS o path (começa com /), NÃO a URL completa!
# O domínio é detectado automaticamente da requisição.
#
# IMPORTANTE: Configure o MESMO valor em VITE_AUTH_CALLBACK_PATH (linha 14)
#
# Este base path cria as rotas:
# - /googleLogin/signin/google (página de login - usada pelo cliente)
# - /googleLogin/callback/google (callback - CONFIGURE NO GOOGLE CONSOLE)
# - /googleLogin/signout (logout)
#
# No Google Console, registre a URI autorizada:
# https://seudominio.com/googleLogin/callback/google
AUTH_GOOGLE_CALLBACK_PATH=/googleLogin
